const { assert, expect } = require('chai');
let ChristmasMovies = require('./02. Christmas Movies_Resources')

describe('Take an array of numbers as an argument', () => {
        let x= {
            movieCollection: [],
            watched: {},
            actors: []
            // "movieCollection"(empty array), "watched" (empty object) and “actors” (empty array).

        }
    it('isSym', () => {
        let chr = new ChristmasMovies
        
        // assert.isArray(this.movieCollection([]), undefined)
        // expect(new ChristmasMovies('aaa')).to.be.

    })
})


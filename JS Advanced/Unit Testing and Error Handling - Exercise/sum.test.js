let sum = require('./04.SumNumbers.js')
const { expect } = require('chai');

describe('Take an array of numbers as an argument', () =>{
    it('testing', () =>{

        expect.sum(arr).to.be.an(Array)
    })
})